import { RainbowHoverDirective } from './rainbow-hover.directive';

describe('RainbowHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowHoverDirective();
    expect(directive).toBeTruthy();
  });
});
